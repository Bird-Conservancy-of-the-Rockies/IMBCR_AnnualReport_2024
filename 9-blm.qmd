---
format: 
  html:
    toc: true
    toc-depth: 4
---
```{r echo = FALSE, message = FALSE, warning = FALSE}

library(tidyverse)
library(xfun) # numbers_to_words function
library(lubridate)


year <- 2024

data <- read.csv(paste0("data/", year, "_annualreportdata.csv"))

  data$FirstVisit <- as_date(data$FirstVisit)
  data$FirstVisit <- paste(month(data[,"FirstVisit"], 
                           label = TRUE, abbr = FALSE), 
                           day(data[,"FirstVisit"]), 
                           sep = " ")
  data$LastVisit <- as_date(data$LastVisit)
  data$LastVisit <- paste(month(data[,"LastVisit"], 
                          label = TRUE, abbr = FALSE), 
                          day(data[,"LastVisit"]), 
                          sep = " ")

```

# Bureau of Land Management 

## BLM California 

### BLM in California BCR 9 
 
```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CA-BCR9-CC is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CA-BCR9-CC")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CA-BCR9-NC is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CA-BCR9-NC")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR10-BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR10-BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-CR is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-CR")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-GJ is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-GJ")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-GU is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-GU")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BLM - Kremmling Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BLM - Kremmling Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BLM - Little Snake Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BLM - Little Snake Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-RG is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-RG")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-SL is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-SL")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-TR is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-TR")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BCR16-UN is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BCR16-UN")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# CO-BLM - White River Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "CO-BLM - White River Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR10-BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR10-BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR9-BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR9-BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR9-BR is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR9-BR")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR9-BU is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR9-BU")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Challis Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Challis Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR10-CA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR10-CA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Cottonwood Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Cottonwood Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Four Rivers Field Office and Morley Nelson Birds of Prey NCA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Four Rivers Field Office and Morley Nelson Birds of Prey NCA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR9-JA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR9-JA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BCR9-OW is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BCR9-OW")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Pocatello Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Pocatello Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Salmon Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Salmon Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Shoshone Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Shoshone Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ID-BLM - Upper Snake Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ID-BLM - Upper Snake Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# MT-BCR10-BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "MT-BCR10-BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# MT-BCR11-BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "MT-BCR11-BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# MT-BCR17-BL is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "MT-BCR17-BL")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::


## BLM Nevada 

### BLM in Nevada BCR 9 
 
```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# NV-BCR9-BM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "NV-BCR9-BM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# NV-BCR9-CC is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "NV-BCR9-CC")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# NV-BCR9-EK is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "NV-BCR9-EK")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# NV-BCR9-EY is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "NV-BCR9-EY")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# NV-BCR9-NC is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "NV-BCR9-NC")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# NV-BCR9-WI is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "NV-BCR9-WI")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::


## BLM North Dakota 

### BLM in North Dakota BCR 17 
 
```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# ND-BCR17-BM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "ND-BCR17-BM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::


## BLM Oregon 

### BLM Oregon in BCR 10 
 
```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR10 BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR10 BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR10-BU is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR10-BU")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR10-PR is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR10-PR")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR10-VA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR10-VA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::



### BLM Oregon in BCR 9 
 
```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR9 BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR9 BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR9-BU is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR9-BU")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR9-LA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR9-LA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR9-PR is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR9-PR")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# OR-BCR9-VA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "OR-BCR9-VA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::


## BLM South Dakota 

### BLM in South Dakota BCR 17 
 
```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# SD-BCR17-BM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "SD-BCR17-BM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR10 BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR10 BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR16 BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR16 BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR9 BLM is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR9 BLM")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BLM - Cedar City Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BLM - Cedar City Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BLM - Fillmore Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BLM - Fillmore Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR16-GS is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR16-GS")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR16-KA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR16-KA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR16-MO is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR16-MO")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR16-MN is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR16-MN")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BCR16-PR is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BCR16-PR")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BLM - Richfield Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BLM - Richfield Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BLM - Saint George Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BLM - Saint George Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BLM - Salt Lake Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BLM - Salt Lake Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# UT-BLM - Vernal Field Office is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "UT-BLM - Vernal Field Office")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR16-BL is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR16-BL")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR18-BL is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR18-BL")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BLM-Buffalo is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BLM-Buffalo")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BLM-Casper is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BLM-Casper")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-CO is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-CO")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-KE is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-KE")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-LA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-LA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR17-NE is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR17-NE")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-PI is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-PI")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-RA is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-RA")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-RO is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-RO")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::




```{r, echo = FALSE, message = FALSE}
# This script should not need to be updated year to year!

# WY-BCR10-WO is a special value from the heddlr_loop.qmd file
x <- which(data$stratum == "WY-BCR10-WO")

name <- data[x, "name"]
compilation <- ifelse(data[x, "Strata"] > 1, "compiling and jointly", "compiling and")
n.strata <- if (data[x, "Strata"] <= 10) {numbers_to_words(data[x, "Strata"])
  } else data[x, "Strata"]
stratum_plural <- ifelse(data[x, "Strata"] > 1, "strata", "stratum")
completed.grids <- (data[x, "CompletedGrids"])
planned.grids <- (data[x, "PlannedGrids"])
percent.completed <- round((completed.grids/planned.grids)*100)

transect.clause <- if (planned.grids == completed.grids & planned.grids > 2) {
  paste("all")
  } else if (planned.grids == completed.grids & planned.grids == 2) {paste("both")
  } else {paste((completed.grids), 
             "of", 
             (planned.grids),
             sep = " ")}

year <- data[x, "Year"]
completed.points <- data[x, "CompletedPoints"]
first.visit <- data[x, "FirstVisit"]
last.visit <- data[x, "LastVisit"]
n.species <- data[x, "nBirdSpecies"]
n.priority <- data[x, "nPriority"]
n.density <- data[x, "DensitySpeciesCount"]
n.density.priority <- data[x, "DensityPrioritySpecies"]
n.density.robust <- data[x, "DensityRobustCV"]
n.occupancy <- data[x, "OccupancySpeciesCount"]
n.occupancy.priority <- data[x, "OccupancyPrioritySpecies"]
n.occupancy.robust <- data[x, "OccupancyRobustCV"]
type <- if (data[x, "is_superstratum"]) {"Superstratum"} else {"Stratum"}
super_stratum <- if (data[x, "is_superstratum"]) {data[x, "stratum"]} else {data[x, "LongStratum"]}

p1 <- str_glue("We obtained results for {name} by {compilation} analyzing data from {n.strata} {stratum_plural}.")

p2 <- str_glue("Field technicians completed {transect.clause} planned surveys ({percent.completed}%) in {year}. Technicians conducted {completed.points} point counts within the {completed.grids} surveyed grid cells between {first.visit} and {last.visit}. They detected {n.species} bird species, including {n.priority} priority species.")

p3 <- str_glue("Bird Conservancy estimated densities (birds per square kilometer) for {n.density} species that were detected in any year during which surveys were conducted, {n.density.priority} of which are priority species. The data yielded robust density estimates (CV < 50%) for {n.density.robust} species.")

p4 <- str_glue("Bird Conservancy estimated the proportion of occupied plots throughout {name} for {n.occupancy} species that were detected in any year during which surveys were conducted, {n.occupancy.priority} of which are priority species. The data yielded robust occupancy estimates (CV < 50%) for {n.occupancy.robust} species.")

p5 <- str_glue("To view a map of survey locations, density, occupancy, & trend results and species counts within {name} across all years of the project, visit the [RMADC](https://apps.birdconservancy.org/rmadc/).")

head <- paste("####", name, sep = " ")

```

`r head`

`r p1`

`r p2`

`r p3`

`r p4`

::: {.callout-note appearance="minimal"}
## View results on the Rocky Mountain Avian Data Center
`r p5`  

On the Explore the Data tab, select **`r super_stratum`** from the **`r type`** drop-down filter.
:::

